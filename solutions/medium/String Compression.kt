// Title: String Compression
            // Difficulty: Medium
            // Language: Kotlin
            // Link: https://leetcode.com/problems/string-compression/

            }
            chars[w] = prev_char
            if(count > 1){
                for(c in count.toString()){
            }
                    chars[w] = c
                }
                r +=1
                count+=1
            while(r < n  && prev_char == chars[r]){
            val prev_char = chars[r]
        }
            var count = 0
        while( r < n ){
            w++
                    w++
